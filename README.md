# Birthday Notifyre

Приложение для автоматических уведомлений о днях рождения через Telegram.

## Функционал

- Автоматическая проверка дней рождения каждый день в 9:00
- Отправка уведомлений в Telegram при обнаружении дней рождения
- REST API для управления друзьями
- Возможность ручного запуска проверки дней рождения

## Технологии

- Spring Boot 3.5.5
- Spring Data JPA
- PostgreSQL
- Liquibase
- Telegram Bot API
- Spring Scheduling

## Настройка

### Переменные окружения

Создайте файл `.env` или установите следующие переменные окружения:

```bash
DB_URL=jdbc:postgresql://localhost:5432/birthday_db
DB_USERNAME=your_username
DB_PASSWORD=your_password
TELEGRAM_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

### Создание Telegram бота

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Получите токен бота
5. Добавьте бота в нужный чат
6. Получите chat_id чата (можно использовать @userinfobot)

## Запуск

```bash
mvn spring-boot:run
```

## API Endpoints

### Управление друзьями

- `GET /api/friend/all` - Получить всех друзей
- `GET /api/friend/lastname/{lastname}` - Найти друга по фамилии
- `POST /api/friend/create` - Создать нового друга
- `GET /api/friend/birthdays/today` - Получить друзей с днем рождения сегодня
- `POST /api/friend/birthdays/check` - Запустить проверку дней рождения вручную

### Пример создания друга

```bash
curl -X POST "http://localhost:8080/api/friend/create" \
  -H "Content-Type: application/json" \
  -d '{
    "firstName": "Иван",
    "middleName": "Иванович",
    "lastName": "Иванов",
    "birthDate": "1990-05-15"
  }'
```

## Автоматические уведомления

Приложение автоматически проверяет дни рождения каждый день в 9:00 и отправляет уведомления в Telegram в формате:

```
🎉 Сегодня день рождения у Иван Иванович Иванов! 🎂
Поздравьте с праздником!
```

## Тестирование

Запустите тесты:

```bash
mvn test
```

Для ручного тестирования функционала:

1. Добавьте друга с днем рождения на сегодняшнюю дату
2. Вызовите эндпоинт `POST /api/friend/birthdays/check`
3. Проверьте получение уведомления в Telegram

## Структура базы данных

Таблица `friend`:
- `id` - первичный ключ
- `first_name` - имя
- `middle_name` - отчество
- `last_name` - фамилия
- `birth_date` - дата рождения

